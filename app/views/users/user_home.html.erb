<div class="user_home">
  <%= render "home/header"%>

  <div class="welcome_msz">
   <h1>Hello!</h1>
   <h1><%= current_user.first_name %></h1>
  </div>
  <% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
<% end %>
  <% if can? :create,:update, @books%>
    <div>
      <%= link_to "Create Book", new_book_url %>
    </div>
    <div>
      <%= link_to "Borrowings", borrowings_path %>
    </div>
  <% end %>

  <%= render "home/all_book" , locals: { book: @book } %>
 
</div>

